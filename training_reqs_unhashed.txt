# docker/training/requirements.txt
#
# Dependencies for the projectdavid training container.
# Installed into the pytorch/pytorch:2.3.0-cuda12.1-cudnn8-devel base image.
#
# This file is intentionally separate from api_requirements.in —
# the training container is a different runtime environment (GPU, CUDA,
# training frameworks) with no overlap with the FastAPI service.
#
# Build:
#   docker build -t projectdavid/training:latest -f docker/training/Dockerfile .

# ------------------------------------------------------------------
# Extra index for PyTorch CUDA 12.1 wheels
# (base image already has torch — listed here so pip resolves
#  compatible versions of dependent packages correctly)
# ------------------------------------------------------------------
--extra-index-url https://download.pytorch.org/whl/cu121

# ------------------------------------------------------------------
# HuggingFace stack
# ------------------------------------------------------------------
huggingface_hub
transformers>=4.40.0,<5.0.0
datasets>=2.18.0
tokenizers>=0.19.0
sentencepiece
accelerate>=0.29.0

# ------------------------------------------------------------------
# Fine-tuning frameworks
# ------------------------------------------------------------------
peft>=0.10.0
trl>=0.8.0
bitsandbytes>=0.43.0

# ------------------------------------------------------------------
# Axolotl — installed via git in Dockerfile to pin exact tag.
# Listed here for documentation only.
# axolotl @ git+https://github.com/OpenAccess-AI-Collective/axolotl.git@v0.4.1
# ------------------------------------------------------------------

# ------------------------------------------------------------------
# Unsloth — installed via git in Dockerfile with graceful fallback.
# Listed here for documentation only.
# unsloth @ git+https://github.com/unslothai/unsloth.git
# ------------------------------------------------------------------

# ------------------------------------------------------------------
# Worker dependencies
# ------------------------------------------------------------------
redis>=5.0.0
httpx>=0.27.0

# ------------------------------------------------------------------
# Config and utilities
# ------------------------------------------------------------------
PyYAML>=6.0
rich>=13.0.0

# ------------------------------------------------------------------
# FastAPI dependencies
# ------------------------------------------------------------------
fastapi
uvicorn[standard]
httpx
redis
python-multipart
pydantic
# ------------------------------------------------------------------
# Internal dependencies
# ------------------------------------------------------------------
projectdavid-common
# ------------------------------------------------------------------
# DB dependencies
# ------------------------------------------------------------------
SQLAlchemy>=2.0.0
sqlalchemy
