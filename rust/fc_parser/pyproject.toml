[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "fc_parser"
version = "0.1.0"
description = "Rust-accelerated <fc> tag parser for projectdavid-core tool call dispatch"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
